import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  main: { fontSize: 18 },
  container: { paddingVertical: 5, paddingRight: 5 },
  red: { color: 'red' },
  blue: { color: 'blue' },
  green: { color: 'green' }
});

export default styles;
