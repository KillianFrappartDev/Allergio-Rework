import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { backgroundColor: '#FFF', flex: 1, alignItems: 'center' },
  searchContainer: { marginTop: 60, marginBottom: 20, justifyContent: 'center', flexDirection: 'row' },
  search: { backgroundColor: '#FFF', padding: 0, width: '90%', height: 50, borderRadius: 5 },
  center: { flex: 1, justifyContent: 'center', alignItems: 'center' }
});

export default styles;
