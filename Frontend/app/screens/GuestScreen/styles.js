import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  signupText: { color: '#6CDCA1' },
  signupButton: {
    borderColor: '#6CDCA1',
    borderWidth: 1,
    width: 200,
    height: 40,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 30
  }
});

export default styles;
